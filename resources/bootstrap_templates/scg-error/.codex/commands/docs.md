# /docs <query>

Search project documentation.

Uses `docs.search` MCP tool if available, otherwise searches markdown files.

Examples:
- `/docs authentication flow`
- `/docs error handling`
- `/docs API rate limiting`

Return:
- Relevant documentation sections
- File paths
- Key information
