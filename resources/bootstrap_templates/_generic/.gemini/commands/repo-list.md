# /repo-list [language]

Use MCP tool `scg_repo_list`.

Inputs:
- optional `language` filter (for example: `go`)

Return:
- repo count
- ordered repo slugs
- any outliers (missing cmd/, missing scripts/, unusual structure)
