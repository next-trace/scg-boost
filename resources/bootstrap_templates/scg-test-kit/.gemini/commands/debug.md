# /debug <issue>

Use `debug-assistant` agent to diagnose production issues.

Provide a description of the issue to investigate.

Examples:
- `/debug API returning 500 errors on /users endpoint`
- `/debug Token validation failing intermittently`
- `/debug High memory usage in gateway service`

Return:
- Problem summary
- Root cause hypothesis
- Evidence
- Recommended fix
