# /triage <problem>

Use `repo-radar` to locate the smallest set of relevant files for <problem>.
Return:
- likely root cause hypotheses
- exact files to inspect/change
- next minimal action

Rules:
- No refactor proposals unless asked.
- No large code blocks; only targeted snippets.
