# /contract-check <change-summary>

Use `contract-guard` to identify any breaking changes implied by <change-summary>.
Return:
- contract risks
- impacted consumers
- mitigation (versioning / adapter / backward-compatible plan)
